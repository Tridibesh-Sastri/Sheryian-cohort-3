## Constructive Distraction
- I'm going to build a custom exoskeleton suit with all of the capabilities of flying, pulling, and increasing human strength, all of those things, and also stay updated with the latest tech around this. It will be always counted as the top-notch performed exoskeleton suit in the world.  
  
If I am starting from the beginning, what are the things that I should learn for it? How should I go about it? What are the things that I should follow to stay updated with time?

- {Side hustle} simple doable tasks on upowrk and other parttime platforms
### System thinking

### Other project
### Ongoing Task

## Specials / Extra (revise tagging)
- ES link file custom writing rules in industries
## General Discussion
    -Test
## Core Content 
    - Functions
        - definition
            - reusible 
            - readability
            - maintanibility
            - acessebility
            - modularity
    - function declaration 
    - functions expression
    - **comparison betwenn function declaration, expression, arrow **
    - **ES link file custom writing rules in industries**
    - IIFE (Immidiately Invoked function expression)
    - ** next test question**
        (()=>console.log("Hellow World!")) // valid IIFE
    - when function have no return then the return is undefined
    default return is undefined
    - ** In oneliner function we can't use return keyword but it can return directly cause to return we need to use curly braces then it will not anymore a inliner functions[01:58:30] updated [02:25:30]
    - What is Global execution from scopes pov
    - let restOperator = (...arr)  =>{.....}
    - default parameter of argument in funcitons 
        var faa = (user = "Ramu", age = 39){console.log(Ramu +"'s age "+age)};

        faa("gandu",54);
        faa();
        faa("gandu");
        faa(undefined,34);

    - **  Callback Function **
    - Functions as first class citizen
    - callback function returning the taken function
    
